'use strict';

/**
 * Normalização dos documentos. A implementação vive em shared/documento-schema.js
 * para ser usada também pelo navegador (modo local, sem servidor).
 */

module.exports = require('../shared/documento-schema');
